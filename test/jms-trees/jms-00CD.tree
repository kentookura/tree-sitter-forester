\title{the induced [monad algebra](jms-00C5)}
\taxon{construction}
\date{2023-10-30}
\author{jonmsterling}
\import{base-macros}
\meta{source}{\citek{joyal-moerdijk-1995}}

\import{jms-00C6}

\def\ToMonadAlg[X]{#{\bar{\X}}}

\let\T{#{T}}
\let\TT{#{\mathbb{T}}}
\let\C{#{\mathcal{C}}}
\let\X{#{X}}
\let\pred[Z]{#{\pi\Sub{\Z}}}

\p{Let \TT be a monad on a category \C, and let \X be a [weak algebra](jms-00C8) for \TT that is a fixed point, i.e. the algebra map is invertible by \Mor{\pred{\X}}{\X}{\T\X}. We may define the \em{induced #{\TT}-algebra} \ToMonadAlg{\X} to have the same carrier object as \X and with the algebra map \Mor{\alg{\ToMonadAlg{\X}}}{\T\X}{\X} defined as follows:

  \texfig{
    \begin{tikzpicture}[scale=0.5]
      \path
        coordinate (nw)
        ++(1,0) coordinate[label=above:$\X$] (n/X)
        ++(5,-4) coordinate[label=below:$\X$] (s/X)
        ++(1,0) coordinate (se);
      \draw[spath/save=wire] (n/X) to[out=-90,in=90] (s/X);
      \path
        coordinate[dot,label=left:$\pred{\X}$] (pi) at (spath cs:wire 0.15)
        coordinate[dot,label=-135:$\alg{\X}$] (alg) at (spath cs:wire 0.8)
        coordinate[label=above:$\T$] (n/T) at (nw -| alg)
      ;
      \draw[spath/save=vert] (alg) to (n/T);
      \path coordinate[dot,label=right:$\mu$] (mu) at (spath cs:vert 0.4);
      \draw (pi.center) to (mu.center);
      \begin{scope}[on background layer]
        \fill[catc] (n/X) to[spath/use={wire,weld}] (s/X) to (se) to (nw -| se) to cycle;
      \end{scope}
    \end{tikzpicture}
  }
}

