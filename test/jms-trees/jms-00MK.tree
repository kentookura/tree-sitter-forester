\title{non-empty subsets of [enumerable](jms-00ME) sets are [enumerable](jms-00ME)}
\date{2024-01-29}
\author{jonmsterling}
\contributor{marcelofiore}
\taxon{lemma}
\import{jms-00JB}

\p{Any non-empty subset of an [enumerable](jms-00ME) set is [enumerable](jms-00ME).}

\proof{
 \p{We will use the same method as in our proof of \ref{jms-00MI}.}
 \p{Let #{S\subseteq A} be a non-empty subset of an [enumerable](jms-00ME) set #{A}, so that we have some #{s\in S} and a [surjection](jms-00M8) #{e\colon\mathbb{N}\twoheadrightarrow A}. We define #{e'\colon \mathbb{N}\twoheadrightarrow{S}} to send #{i\in\mathbb{N}} to #{e\prn{i}} if #{e\prn{i}\in S} and to #{s} otherwise.}
}